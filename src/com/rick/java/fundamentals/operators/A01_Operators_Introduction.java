package com.rick.java.fundamentals.operators;

/**
 * Author: Rick_Richards
 * 
 * @since 26 feb 2026
 */

public class A01_Operators_Introduction {
	/*
	 * ========================================================= JAVA OPERATORS
	 * =========================================================
	 *
	 * 1) ARITHMETIC OPERATORS
	 * --------------------------------------------------------- + Addition -
	 * Subtraction * Multiplication / Division % Modulus (remainder) ++ Increment --
	 * Decrement
	 *
	 *
	 * 2) ASSIGNMENT OPERATORS
	 * --------------------------------------------------------- = Simple assignment
	 * += Add and assign -= Subtract and assign *= Multiply and assign /= Divide and
	 * assign %= Modulus and assign &= Bitwise AND and assign |= Bitwise OR and
	 * assign ^= Bitwise XOR and assign <<= Left shift and assign >>= Right shift
	 * and assign >>>= Unsigned right shift and assign
	 *
	 *
	 * 3) RELATIONAL (COMPARISON) OPERATORS
	 * --------------------------------------------------------- == Equal to != Not
	 * equal to > Greater than < Less than >= Greater than or equal to <= Less than
	 * or equal to
	 *
	 *
	 * 4) LOGICAL OPERATORS
	 * --------------------------------------------------------- && Logical AND ||
	 * Logical OR ! Logical NOT
	 *
	 *
	 * 5) BITWISE OPERATORS
	 * --------------------------------------------------------- & Bitwise AND |
	 * Bitwise OR ^ Bitwise XOR ~ Bitwise Complement
	 *
	 *
	 * 6) SHIFT OPERATORS ---------------------------------------------------------
	 * << Left shift >> Signed right shift >>> Unsigned right shift
	 *
	 *
	 * 7) TERNARY OPERATOR ---------------------------------------------------------
	 * ? : Conditional operator Example: condition ? valueIfTrue : valueIfFalse;
	 *
	 *
	 * 8) INSTANCEOF OPERATOR
	 * --------------------------------------------------------- instanceof Checks
	 * if an object is an instance of a class
	 *
	 *
	 * 9) TYPE CASTING OPERATOR
	 * --------------------------------------------------------- (type) Explicit
	 * casting Example: int x = (int) 10.5;
	 *
	 *
	 * ========================================================= Notes: - Operators
	 * allow us to perform calculations, comparisons, logical decisions, and
	 * bit-level manipulations. - Understanding operator precedence is important
	 * when combining multiple operators in one expression.
	 * =========================================================
	 */
	public static void main(String[] args) {
		System.out.println("Welcome to introduction to Operators in java:");

	}

}
